apiVersion: v1
kind: Service
metadata:
  name: {{ include "graph-metadata-dashboard.fullname" . }}
  labels:
    {{- include "graph-metadata-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "graph-metadata-dashboard.selectorLabels" . | nindent 4 }}
